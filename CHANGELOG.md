# Changelog

Все важные изменения в этом проекте документируются в этом файле.

## [2.0.0] - 2025-12-10

### ✨ Добавлено

- **Новая система конфигурации**
  - `config.yaml` с полной настройкой JQL, фильтров, интервалов
  - `config.py` с классом `Config` для загрузки и парсинга YAML
  - Поддержка переменных окружения (`.env`)
  - Feature-toggles для включения/выключения функций

- **CLI интерфейс**
  - `cli.py` с аргументами: `--config`, `--dry-run`, `--once`, `--log-level`, `--no-telegram`, `--no-time-control`, `--interval`
  - Улучшенная справка и примеры использования
  - Graceful shutdown на `Ctrl+C`

- **Reporting модуль**
  - `reporting.py` с классом `JiraReporter`
  - Экспорт в CSV и Markdown
  - Метрики по статусам, создателям, приоритетам
  - Автоматическое расписание отчётов
  - Интеграция с Telegram (`Daily Report` кнопка)

- **Telegram handlers**
  - `telegram_handlers.py` с классом `TelegramHandlers`
  - Отдельный модуль для обработки команд
  - Новая команда `Daily Report` с метриками
  - Авторизация по `main_id`

- **Кэширование задач**
  - `processed_issues_cache` с TTL (время жизни)
  - Предотвращение дубликатов уведомлений
  - Настраиваемый TTL для разных типов задач

- **Dry-run режим**
  - Флаг `--dry-run` для безопасного тестирования
  - Логирование всех действий без реальных изменений в Jira

- **Улучшенное логирование**
  - Ротируемые логи в файл (`logs/jira_updater.log`)
  - Настройка уровня логирования через config.yaml
  - Одновременный вывод в консоль и файл

- **Unit тесты**
  - `tests/test_jira_updater.py` с тестами skip-логики
  - Тесты кэширования и list-extraction
  - Fixtures для DummyJira и DummyBot

- **Документация**
  - Расширенный README.md с примерами
  - Разделы по установке, конфигурации, troubleshooting, FAQ
  - Диаграмма архитектуры и потока данных
  - Примеры cron-команд

### 🔄 Изменено

- **Рефакторинг основного модуля** (`test.py`)
  - Переработка `JiraTaskUpdater` в класс с явным состоянием
  - Разделение логики на методы: `_process_new_issues_batch()`, `_process_updates_batch()`, `_assign_issue()` и т.д.
  - Убрано дублирование кода (единая функция `_get_list()` вместо разных для разных запросов)
  - Внедрена `Config` для гибкой конфигурации

- **Переименование и реорганизация**
  - `variable` → `running_main_loop`
  - `variable_time` → `running_by_time`
  - Методы `_toggle_main_loop()`, `_toggle_by_time()` для явного управления состоянием

- **Legacy поддержка**
  - `main.py` сохранён для backward compatibility
  - Автоматический fallback на `dist.secrets` если нет config.yaml

- **CLI как основной entry point**
  - Рекомендуется использовать `python cli.py` вместо `python test.py`
  - Альтернатива: `python main.py` для легаси-режима

### 🐛 Исправления

- Устранено дублирование кода при получении списков задач
- Исправлена обработка ошибок в Telegram-отправках (не падает при отсутствии бота)
- Улучшена обработка исключений при подключении к Jira
- Graceful shutdown при `Ctrl+C` (корректное завершение потоков)

### 🔒 Безопасность

- Токены хранятся в `.env` и `.gitignore` скрывает файл
- Конфиг может содержать чувствительные данные только через env-переменные
- Логирование не выводит токены

### ⚠️ Breaking Changes

Нет breaking changes для конечного пользователя:
- Старый `test.py` работает как раньше
- `main.py` совместим с `dist/secrets`
- Новые функции опциональны (feature-toggles)

---

## [1.0.0] - 2024 (Оригинальная версия)

### ✨ Начальные возможности

- Автоматический поиск новых задач в Jira
- Переведение задач в статус "В работе"
- Ротационное распределение между двумя исполнителями
- Уведомления в Telegram
- Отслеживание обновлений в watch-листе
- Фильтрация по ключевым словам и создателям
- Time-based контроль (sleep/wake)
- Многопоточность для параллельной работы

---

## Планы на будущее

- [ ] WebHook поддержка (вместо поллинга)
- [ ] Database хранилище для метрик
- [ ] Web UI для управления конфигом
- [ ] Поддержка multiple проектов одновременно
- [ ] Интеграция с другими мессенджерами (Slack, Teams)
- [ ] REST API для интеграции
- [ ] Docker контейнеризация
- [ ] GitHub Actions для автоматизированного тестирования
